version: '3.8'

services:
  dhcp-server:
    image: strm/dnsmasq:latest
    container_name: dhcp-server
    restart: always
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ./dnsmasq.conf:/etc/dnsmasq.conf
