services:
  ntp:
    image: dockurr/chrony:latest
    container_name: ntp_chrony
    restart: unless-stopped
    ports:
      - "123:123/udp"
    cap_add:
      - SYS_TIME
    volumes:
      - ./chrony.conf:/etc/chrony.conf:ro
