services:
  web-ftp:
    image: nginx:alpine
    container_name: ftpdocker_web
    ports:
      - "8080:80"
    volumes:
      - ./data:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./.htpasswd:/etc/nginx/.htpasswd:ro
    restart: always
